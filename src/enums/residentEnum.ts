export enum SexEnum {
  MALE = 0,
  FEMALE = 1,
}

export enum StatusEnum {
  NO = 0,
  YES = 1,
}

export enum VehicleEnum {
  CAR = 0,
  BUS = 1,
  TRAIN = 2,
  CHSR = 3,
  PLANE = 4,
}

export enum HealthEnum {
  LOW = 0,
  MIDDLE = 1,
  HIGH = 2,
}
